# 📝 گزارش تغییرات HvM

## 🚀 نسخه 0.9.24 alpha — 📅 ۱۴۰۵/۰۴/۰۸ | 2026-06-29

### 🧮 پایه سنوات

- محاسبه پایه سنوات ۱۴۰۵ با جدول بیدبرگ بر اساس «تعداد سال سابقه کامل» هماهنگ شد.
- برای شروع کار `۱۳۸۹/۰۷/۲۴`، تا `۱۴۰۵/۰۷/۲۳` سابقه کامل ۱۵ سال و نرخ روزانه `۱٬۷۹۳٬۴۷۲` ریال محاسبه می‌شود.
- از روز `۱۴۰۵/۰۷/۲۴` سابقه کامل ۱۶ سال و نرخ روزانه `۱٬۸۲۱٬۲۳۴` ریال اعمال می‌شود.
- ماه سالگرد همچنان به‌صورت تفکیکی روزهای قبل و بعد از سالگرد محاسبه می‌شود.

### 🧪 کیفیت و انتشار

- نسخه برنامه به `0.9.24 alpha` ارتقا یافت.
- این نسخه همچنان pre-release است.

## 🚀 نسخه 0.9.23 alpha — 📅 ۱۴۰۵/۰۴/۰۸ | 2026-06-29

### 🧮 پایه سنوات

- جدول پایه سنوات تجمیعی ۱۴۰۵ با جدول صحیح ارسال‌شده جایگزین شد.
- محاسبه پایه سنوات برای کارمندانی که تاریخ شروع کارشان وسط سال است، تا قبل از روز سالگرد با نرخ قبل از سالگرد و از همان روز سالگرد با نرخ جدید انجام می‌شود.
- برای نمونه، شروع کار `۱۳۸۹/۰۷/۲۴` در فروردین ۱۴۰۵ با نرخ روزانه `۱٬۶۵۴٬۵۴۹` ریال و مبلغ ماهانه `۵۱٬۲۹۱٬۰۱۹` ریال محاسبه می‌شود.

### 🧪 کیفیت و انتشار

- نسخه برنامه به `0.9.23 alpha` ارتقا یافت.
- این نسخه همچنان pre-release است.

## 🚀 نسخه 0.9.22 alpha — 📅 ۱۴۰۵/۰۴/۰۸ | 2026-06-29

### 🧮 پایه سنوات

- پایه سنوات ۱۴۰۵ در زمان ثبت و ویرایش هر فیش، بر اساس تاریخ شروع کار و دوره همان فیش محاسبه می‌شود.
- افزایش سالانه دقیقاً از روز سالگرد شروع کار اعمال می‌شود و مبلغ ماه سالگرد از روزهای قبل و بعد از سالگرد به‌صورت تفکیکی محاسبه می‌شود.
- نرخ روزانه سنوات در فیش قابل ویرایش است و پس از تغییر، دکمه بازگشت به نرخ قانونی محاسبه‌شده نمایش داده می‌شود.
- فیلدهای پایه سنوات روزانه، ماهانه، سنوات سال قبل و کلید دستی سابقه از فرم کارمند تکی و ورود گروهی حذف شدند؛ تاریخ شروع کار منبع محاسبه است.

### 🔄 سرور و همگام‌سازی

- نرخ دستی سنوات و وضعیت محاسبه خودکار در پیش‌نویس همان ماه ذخیره و میان دستگاه‌ها همگام می‌شود.
- schema، validation و backup/restore سرور با داده‌های جدید پیش‌نویس فیش سازگار شدند.

### 🧪 کیفیت و انتشار

- نسخه اپ، سرور و پنل مدیریت به `0.9.22 alpha` ارتقا یافت.
- این نسخه همچنان pre-release است.

## 🚀 نسخه 0.9.21 alpha — 📅 ۱۴۰۵/۰۴/۰۸ | 2026-06-29

### 🤖 آپدیت اندروید

- APKهای اندروید از این نسخه با کلید release ثابت و ذخیره‌شده در GitHub Secrets امضا می‌شوند تا نصب آپدیت روی نسخه‌های بعدی با خطای signature/package متوقف نشود.
- workflow انتشار در صورت نبودن کلید امضای اندروید متوقف می‌شود و دیگر APK با debug key موقت GitHub runner منتشر نمی‌کند.
- دانلود آپدیت قبل از باز شدن نصب‌کننده اندروید از نظر حجم و معتبر بودن فایل APK بررسی می‌شود تا فایل ناقص یا اشتباه با خطای واضح رد شود.
- خطاهای باز شدن APK در اندروید به پیام‌های قابل فهم‌تر برای کاربر تبدیل شدند.

### 🧪 کیفیت و انتشار

- نسخه برنامه به `0.9.21 alpha` ارتقا یافت.
- این نسخه همچنان pre-release است.

## 🚀 نسخه 0.9.20 alpha — 📅 ۱۴۰۵/۰۴/۰۸ | 2026-06-29

### 📅 تاریخ‌ها

- در منوی انتخاب ماه تقویم، عدد ماه کنار نام ماه نمایش داده می‌شود و بعد از انتخاب فقط نام ماه باقی می‌ماند.
- فیلدهای تاریخ کارمند، وام، مساعده و مرخصی قابل انتخاب متن، کپی و پیست شدند و همچنان انتخاب‌گر تاریخ را باز می‌کنند.

### 💳 پرداخت‌ها

- قفل پرداخت فیش‌ها اضافه شد؛ فیش‌ها برای کاربر بخش پرداخت به‌صورت پیش‌فرض بسته‌اند و فقط بعد از باز شدن قفل توسط ادمین قابل ثبت «پرداخت شد» یا «پرداخت نشد» هستند.
- ادمین می‌تواند قفل پرداخت را تکی یا دسته‌ای با انتخاب چند فیش باز و بسته کند.
- فیلترهای وضعیت پرداخت و وضعیت قفل، sortهای بیشتر، و انتخاب دسته‌ای به صفحه پرداخت اضافه شدند.
- حذف موردی لاگ تاریخچه پرداخت برای ادمین اضافه شد و بعد از حذف، وضعیت فیش بر اساس آخرین لاگ باقی‌مانده بازسازی می‌شود.

### 👤 کارکنان و فیش حقوق

- فرم ویرایش کارمند به بخش‌های مستقل قابل باز و بسته شدن تبدیل شد و در حالت ویرایش بخش‌ها ابتدا بسته نمایش داده می‌شوند.
- برای مقادیر محاسبه‌شده در فرم کارمند، ثبت/ویرایش فیش تکی و ورود دسته‌ای، دکمه بازگشت به مقدار پیش‌فرض برنامه اضافه شد.
- پایه سنوات ۱۴۰۵ با جدول تجمیعی سال استخدام اصلاح شد و در ثبت کارمند جدید بر اساس تاریخ شروع کار، مقدار روزانه و ماهانه خودکار پر می‌شود.

### 🔄 سرور و همگام‌سازی

- schema، sync، validation و backup/restore سرور برای قفل پرداخت و وضعیت ثبت‌شده/ثبت‌نشده پرداخت به‌روزرسانی شد.
- سمت سرور جلوی ثبت وضعیت پرداخت توسط نقش payment در حالت قفل گرفته می‌شود.

### 🧪 کیفیت و انتشار

- نسخه برنامه به `0.9.20 alpha` ارتقا یافت.
- نسخه سرور و پنل مدیریت با `0.9.20-alpha` هماهنگ شد.
- analyzer و همه تست‌های اپ و سرور اجرا و پاس شدند.

## 🚀 نسخه 0.9.12 alpha — 📅 ۱۴۰۵/۰۴/۰۷ | 2026-06-28

### ⚙️ تنظیمات برنامه

- صفحه تنظیمات بازطراحی شد تا هر بخش در کارت مستقل و قابل باز و بسته شدن نمایش داده شود.
- تنظیمات حقوق پایه، ضرایب افزایش، بیمه، مالیات، مرخصی، امنیت، آپدیت، ظاهر، دسترسی‌پذیری، بکاپ و درباره از هم جدا شدند.
- بخش‌های کم‌استفاده‌تر به‌صورت پیش‌فرض جمع می‌شوند تا صفحه خلوت‌تر و قابل اسکن‌تر باشد.
- عرض و فاصله‌گذاری صفحه تنظیمات برای نمای بزرگ ویندوز بهینه‌تر شد.

### 🧪 کیفیت و انتشار

- نسخه برنامه به `0.9.12 alpha` ارتقا یافت.
- متن Release فقط شامل تغییرات همین بیلد است.

## 🚀 نسخه 0.9.11 alpha — 📅 ۱۴۰۵/۰۴/۰۷ | 2026-06-28

### 🔔 نوتیفیکیشن‌های برنامه

- پیام‌های پایین برنامه برای ویندوز و اندروید با طراحی جدید، آیکن، رنگ‌بندی وابسته به نوع پیام و انیمیشن نرم ورود و خروج بازطراحی شدند.
- پیام‌های موفق، خطا، هشدار و اطلاع‌رسانی از یک مسیر مشترک نمایش داده می‌شوند تا ظاهر و رفتار برنامه یکدست باشد.
- پیام‌های وضعیت همگام‌سازی، آپدیت، فیش حقوق، پرداخت‌ها، ورود محلی و عملیات دسته‌ای به سیستم جدید نوتیفیکیشن منتقل شدند.

### 🪟 آپدیت ویندوز

- آپدیتر پرتابل ویندوز با بسته واقعی Release به‌صورت محلی تست شد تا جایگزینی فایل‌های نسخه بعد بدون وابستگی به فایل‌های قدیمی انجام شود.

### 🧪 کیفیت و انتشار

- نسخه برنامه به `0.9.11 alpha` ارتقا یافت.
- متن Release فقط شامل تغییرات همین بیلد است و تاریخ Release با ایموجی 📅 ثبت می‌شود.

## 🚀 نسخه 0.9.10 alpha — ۱۴۰۵/۰۴/۰۷ | 2026-06-28

### 🪟 آپدیت ویندوز

- مکانیزم آپدیت ویندوز به helper مستقل Rust منتقل شد تا نصب بعد از بسته‌شدن برنامه ادامه پیدا کند.
- بعد از تایید نصب، برنامه بسته می‌شود، آپدیت نصب می‌شود و نسخه جدید به‌صورت خودکار دوباره اجرا می‌شود.
- helper پیش از نصب به پوشه موقت منتقل می‌شود تا هنگام جایگزینی فایل‌های برنامه قفل نشود.

### 🚀 انتشار ویندوز

- خروجی ویندوز فقط به‌صورت یک ZIP پرتابل و خودآپدیت‌شونده منتشر می‌شود.
- فایل‌های نصب، feed و پکیج‌های جانبی از Release حذف شدند.
- صفحه Release فقط شامل سه APK اندروید و یک ZIP پرتابل ویندوز است.

### 🧪 کیفیت و انتشار

- نسخه برنامه به `0.9.10 alpha` ارتقا یافت.
- اکشن انتشار روی شاخه `master` اجرا می‌شود و خروجی را به‌صورت pre-release منتشر می‌کند.

## 🚀 نسخه 0.9.9 alpha — ۱۴۰۵/۰۴/۰۷ | 2026-06-28

### 🧾 فیش‌های حقوق

- فیش‌های ذخیره‌شده با تغییر مرخصی، وام یا مساعده به‌صورت خودکار تغییر نمی‌کنند.
- در لیست فیش‌های حقوقی، در صورت تغییر وضعیت مرخصی، وام یا مساعده، دکمه «آپدیت فیش» نمایش داده می‌شود تا کاربر با تایید خودش فیش را به‌روزرسانی کند.
- گزینه «ویرایش فیش» در ستون عملیات فیش‌ها دوباره قابل مشاهده شد و اکشن‌های موبایل بدون بیرون‌زدگی نمایش داده می‌شوند.

### 🖨️ چاپ و خروجی فیش

- خروجی PDF فیش با حاشیه امن بیشتر ساخته می‌شود تا گوشه‌های فیش در پرینترها حذف نشود.
- ردیف «رند حقوق» در پیش‌نمایش و PDF چاپی واضح‌تر شد.
- بخش امضای کارمند، حسابدار و مدیریت به PDF چاپی اضافه شد.

### 🧪 کیفیت و انتشار

- نسخه برنامه به `0.9.9 alpha` ارتقا یافت.
- مراحل جداگانه `flutter pub get` از اکشن انتشار حذف شد و بیلدها مستقیم اجرا می‌شوند.
- اکشن انتشار روی شاخه `master` اجرا می‌شود و خروجی به‌صورت pre-release منتشر می‌شود.

## 🚀 نسخه 0.9.6 — ۱۴۰۵/۰۴/۰۶ | 2026-06-27

### 🧾 چاپ و خروجی فیش

- فیش‌های تکی و دسته‌ای برای اندازه‌های A4، A5 و B5 در حالت عمودی و با اندازه واقعی کاغذ ساخته می‌شوند.
- اعداد داخل خروجی PDF و عکس فیش فارسی شدند تا نمایش و چاپ با زبان برنامه یکدست باشد.
- خروجی عکس فیش‌ها به JPG با پس‌زمینه سفید تغییر کرد و کیفیت رندر خروجی بالاتر رفت.

### 🪟 آپدیت ویندوز

- نصب‌کننده ویندوز بعد از بسته شدن برنامه تا آزاد شدن فایل اجرایی صبر می‌کند و جایگزینی فایل‌ها را با retry انجام می‌دهد.
- بعد از نصب موفق، marker نصب ثبت می‌شود، برنامه دوباره اجرا می‌شود و پیام آپدیت فقط در صورت نصب موفق نمایش داده می‌شود.
- پنجره‌های آپدیت با کلیک بیرون بسته نمی‌شوند و فقط گزینه «بعدا» آن‌ها را می‌بندد.

### 🧾 جدول‌های ویندوز

- عرض پیش‌فرض ستون‌های جدول‌ها متعادل‌تر شد تا متن و عددها کمتر بریده شوند.
- امکان تغییر اندازه ستون‌ها با موس از روی عنوان ستون اضافه شد.

### 🧪 کیفیت و انتشار

- نسخه برنامه به `0.9.6` ارتقا یافت.
- اکشن انتشار روی شاخه `master` اجرا می‌شود و خروجی را به‌صورت pre-release منتشر می‌کند.

## 🚀 نسخه 0.9.5 — ۱۴۰۵/۰۴/۰۶ | 2026-06-27

### 🔐 ورود و ثبت نام

- در اپلیکیشن و پنل مدیریت، امکان نمایش و مخفی کردن رمز عبور به فرم ورود اضافه شد.
- صفحه شروع اپلیکیشن به سه گزینه «ثبت نام»، «ورود» و «پشتیبانی» تغییر کرد.
- درخواست ثبت‌نام کاربران از اپلیکیشن به سرور ارسال می‌شود و بعد از تایید ادمین، ورود خودکار انجام می‌شود.
- نمایش درخواست انتقال دیتای لوکال در اولین ورود حذف شد و داده‌ها همیشه ابتدا از سرور دریافت و ذخیره می‌شوند.

### 🖨️ چاپ و خروجی فیش

- هنگام چاپ فیش تکی یا دسته‌ای، انتخاب اندازه A4، A5 یا B5 اضافه شد.
- خروجی عکس فیش‌ها هم با انتخاب اندازه A4، A5 یا B5 ساخته می‌شود.
- ساخت عکس فیش از اسکرین‌شات صفحه به رندر PDF تغییر کرد تا تمام بخش‌های فیش کامل و هم‌اندازه خروجی چاپ باشد.

### 🔄 نشست و همگام‌سازی

- refresh توکن در اپ و پنل مدیریت قفل هم‌زمانی گرفت تا درخواست‌های موازی باعث خروج ناگهانی یا خطای unauthorized نشوند.
- ورود اول بعد از تایید ثبت‌نام، داده‌های شرکت را از سرور دریافت و در لوکال ذخیره می‌کند.

### 🧪 کیفیت و انتشار

- نسخه برنامه به `0.9.5` ارتقا یافت.
- اکشن انتشار روی شاخه `master` اجرا می‌شود و خروجی را به‌صورت pre-release منتشر می‌کند.

## 🚀 نسخه 0.9.2 — ۱۴۰۵/۰۴/۰۶ | 2026-06-27

### 🪟 آپدیت ویندوز

- مشکل نصب نشدن آپدیت پرتابل ویندوز بعد از بسته‌شدن برنامه رفع شد.
- نصب‌کننده ویندوز حالا هم zipهایی که فایل‌های برنامه در ریشه آن‌ها هستند و هم bundleهای پوشه‌دار را تشخیص می‌دهد.
- بعد از کلیک روی «نصب»، پنجره «در حال نصب» نمایش داده می‌شود و سپس برنامه بسته، فایل‌ها جایگزین و برنامه دوباره اجرا می‌شود.
- اگر نصب به خطا بخورد، به جای بسته‌شدن بی‌صدا، لاگ خطا در پوشه temp نوشته و پیام خطای ویندوز نمایش داده می‌شود.

### 🧪 کیفیت و انتشار

- تست رگرسیون برای اسکریپت نصب‌کننده ویندوز و مقایسه نسخه‌ها اضافه شد.
- نسخه برنامه به `0.9.2` ارتقا یافت.
- اکشن انتشار روی شاخه `master` اجرا می‌شود و خروجی را به‌صورت pre-release منتشر می‌کند.

## 🚀 نسخه 0.9.1 — ۱۴۰۵/۰۴/۰۶ | 2026-06-27

### 🧾 جدول‌های ویندوز

- جدول‌های اصلی دسکتاپ بازطراحی شدند تا ردیف عنوان همیشه هنگام اسکرول عمودی دیده شود.
- سه ستون سمت راست جدول‌ها شامل «ردیف»، «کد» و «نام کارمند» هنگام اسکرول افقی ثابت می‌مانند.
- اسکرول‌بار عمودی و افقی در جدول‌های دسکتاپ همیشه قابل مشاهده هستند.
- اسکرول بدنه ثابت و بدنه متحرک جدول‌ها همگام شد تا ردیف‌ها از هم جدا نیفتند.

### 👤 ستون‌های کارکنان

- نمایش ترکیبی نام و کد کارمند در جدول‌های مساعده، وام و مرخصی به دو ستون جدا تبدیل شد.
- جدول مدیریت کارکنان با ستون‌های «ردیف»، «کد» و «نام کارمند» شروع می‌شود تا با جدول فیش‌های حقوقی هماهنگ باشد.

### 🧪 کیفیت و انتشار

- تست رگرسیون برای رندر جدول دسکتاپ، ستون‌های ثابت و اسکرول‌بارها اضافه شد.
- نسخه برنامه به `0.9.1` ارتقا یافت.
- اکشن انتشار روی شاخه `master` اجرا می‌شود و خروجی را به‌صورت pre-release منتشر می‌کند.

## 🚀 نسخه 0.9.0 alpha - 1405/04/06 | 2026-06-27

### بخش پرداخت

- امکان نمایش و چاپ فیش از داخل کارت‌های بخش پرداخت اضافه شد.
- تاریخچه تغییر وضعیت پرداخت با زمان، کاربر ثبت‌کننده و دلیل پرداخت‌نشدن ذخیره و نمایش داده می‌شود.
- دکمه‌های «پرداخت شد» و «پرداخت نشد» در همه وضعیت‌ها در دسترس می‌مانند تا وضعیت بعدا اصلاح شود.
- کپی سریع مبلغ خالص دریافتی اضافه شد.
- کنترل چیدمان کارت‌ها با تعداد ستون و اندازه کارت برای موبایل و دسکتاپ اضافه شد.

### آپدیت برنامه

- بررسی آپدیت از GitHub به تنظیمات اضافه شد.
- برنامه هنگام باز شدن می‌تواند به‌صورت خودکار آخرین pre-release را بررسی و دانلود کند.
- در ویندوز، آپدیت پرتابل دانلود شده با تایید کاربر نصب و برنامه دوباره اجرا می‌شود.
- در اندروید، APK دانلود شده با تایید کاربر برای نصب باز می‌شود.

### همگام‌سازی و بک‌اند

- history وضعیت پرداخت‌ها با `change_log` در sync، دیتابیس سرور و backup/restore پشتیبانی می‌شود.

## 🚀 نسخه 0.8.5 alpha — ۱۴۰۵/۰۴/۰۶ | 2026-06-27

### 💳 بخش پرداخت

- بخش جدید «بخش پرداخت» به برنامه اضافه شد تا فیش‌های حقوقی ماه‌ها و سال‌های مختلف قابل مشاهده و ثبت وضعیت پرداخت باشند.
- برای هر فیش امکان ثبت «پرداخت‌شده» یا «پرداخت‌نشده» همراه با دلیل پرداخت نشدن اضافه شد.
- ادمین‌ها پس از همگام‌سازی، تغییرات ثبت‌شده توسط کاربر بخش پرداخت را به صورت اعلان داخل برنامه دریافت می‌کنند.

### 🔐 سطح دسترسی

- نقش جدید «بخش پرداخت» در بک‌اند و اپ اضافه شد.
- کاربر بخش پرداخت بعد از ورود فقط صفحه پرداخت و تنظیمات را می‌بیند و داشبورد/نوار پایین اصلی برای او نمایش داده نمی‌شود.
- نقش کاربر هنگام ورود و refresh نشست از سرور خوانده می‌شود.

### 🖥️ پنل مدیریت و بک‌اند

- در ساخت و ویرایش کاربران پنل مدیریت، انتخاب سطح دسترسی ادمین یا بخش پرداخت اضافه شد.
- جدول sync جدید برای وضعیت پرداخت فیش‌ها به دیتابیس سرور، بکاپ شرکت و اعتبارسنجی payload اضافه شد.
- فیلدهای متنی پنل مدیریت جهت نوشتار را بر اساس متن فارسی یا لاتین تنظیم می‌کنند.
- برای رمز موقت صادرشده در پنل مدیریت دکمه کپی اضافه شد.

### 🚀 انتشار

- workflow انتشار برنامه از push روی `master` اجرا می‌شود و همچنان pre-release نسخه را با تگ همان نسخه منتشر می‌کند.

## 🚀 نسخه 0.8.0 alpha — ۱۴۰۵/۰۴/۰۶ | 2026-06-27

### 🎨 ظاهر و تجربه کاربری

- تم مرکزی برنامه با کنترل‌های جدیدتر Material 3 به‌روزرسانی شد و ظاهر دکمه‌ها، سوییچ‌ها، منوها، اسلایدرها و حالت‌های تعاملی یکدست‌تر شد.
- بخش تنظیمات دسترسی‌پذیری با اسلایدرهای جدیدتر و کنترل‌های واضح‌تر برای اندازه متن و اندازه رابط برنامه بازطراحی شد.
- فاصله‌ها، اندازه‌ها و حالت‌های فشرده در ویندوز و اندروید برای جلوگیری از overlap و بریدگی متن اصلاح شدند.

### 📱 ناوبری اندروید

- نوار پایین اندروید به حالت شناور و شفاف با blur، انتخاب‌گر متحرک و انیمیشن روان تغییر کرد.
- نوار پایین روی محتوای صفحه قرار می‌گیرد تا محتوای پشت آن مانند نمونه مرجع دیده شود.
- فضای امن پایین صفحات، دکمه‌های شناور و اسکرول انتهای داشبورد/لیست‌ها تنظیم شد تا محتوا پشت منوی پایین مخفی نماند.

### 🔢 اعداد فارسی

- نمایش اعداد در فرم‌ها، لیست‌ها، جستجوها، نام کارکنان، کدها، تاریخ‌ها و یادداشت‌ها به‌صورت سراسری فارسی‌تر شد.
- formatter مشترک برای تبدیل ارقام انگلیسی و عربی به فارسی در ورودی‌های برنامه اضافه شد.
- ذخیره‌سازی و محاسبات داخلی همچنان با مقدارهای استاندارد انجام می‌شود تا دیتابیس، ورود رمز و فایل‌های خروجی رسمی دچار مشکل نشوند.

### 🧪 کیفیت و پایداری

- مشکل خالی‌شدن بدنه صفحات اندروید پس از اضافه‌شدن منوی پایین رفع شد.
- خطای احتمالی `setState` پس از خروج از صفحه محاسبه حقوق اصلاح شد.
- تست‌های منوی اندروید، فضای امن پایین، فارسی‌سازی اعداد و حفظ وضعیت صفحات اضافه/به‌روزرسانی شدند.
- اجرای موفق `flutter analyze` و همه تست‌های برنامه.

## 🚀 نسخه 0.7.0 alpha — ۱۴۰۵/۰۴/۰۳ | 2026-06-24

### 📱 تجربه اندروید

- وضعیت همگام‌سازی در موبایل به آیکون رنگی کنار انتخاب تم منتقل شد و خطاها از همان بخش قابل مشاهده هستند.
- نوارهای تکراری عنوان و دکمه‌های بازخوانی موبایل حذف شدند و تازه‌سازی با کشیدن صفحه به پایین انجام می‌شود.
- فیلتر و مرتب‌سازی در نمایش موبایل به‌صورت جمع‌شونده درآمدند تا فضای بیشتری برای اطلاعات باقی بماند.
- کارت‌های آماری داشبورد در موبایل فشرده و دو ستونه شدند.

### 📊 داشبورد و جدول‌ها

- چیدمان، فاصله‌ها، نمایش مبلغ و واحد ریال و ارتفاع کارت‌های داشبورد در ویندوز و اندروید اصلاح شد.
- جستجو به فیش‌های حقوقی اضافه شد و خلاصه مبالغ بر اساس نتایج جستجو محاسبه می‌شود.
- ستون ردیف به مرخصی‌ها، وام‌ها و مساعده‌ها اضافه شد.
- ستون و جهت مرتب‌سازی همه جدول‌ها پس از خروج از صفحه و اجرای دوباره برنامه حفظ می‌شوند.

### 🩺 مرخصی و همگام‌سازی

- مرخصی‌های تکراری محلی و سروری پاک‌سازی شدند و ثبت دوباره بازه یکسان برای یک کارمند مسدود شد.
- همگام‌سازی رکوردهای قدیمی مرخصی بدون ایجاد نسخه تکراری اصلاح شد.
- کد پرسنلی کنار نام کارکنان در مرخصی و مساعده نمایش داده می‌شود.

### 🧪 کیفیت و پایداری

- overflowهای داشبورد، چیدمان‌های فشرده و حالت بزرگ‌نمایی متن ویندوز اصلاح شدند.
- تست‌های رابط موبایل، داشبورد و ماندگاری مرتب‌سازی اضافه شدند.
- اجرای موفق `flutter analyze` و همه تست‌های برنامه.

## 🚀 نسخه 0.6.0 alpha — ۱۴۰۵/۰۴/۰۳ | 2026-06-24

### 🧮 محاسبه حقوق

- ذخیره خودکار تغییرات محاسبه تکی و دسته‌ای و بازیابی آن‌ها برای دوره‌های بعدی اضافه شد.
- امکان معاف‌کردن حق مسکن، حق خواروبار و پایه سنوات فراهم شد.
- ترتیب گزینه‌های اضافه‌کاری و نوبت‌کاری و پایداری فرم هنگام همگام‌سازی اصلاح شد.
- از ثبت فیش تکراری جلوگیری شد و خطاها با پیام قابل فهم نمایش داده می‌شوند.

### 🩺 مرخصی، وام و مساعده

- ثبت مرخصی برای دوره‌های گذشته و آینده و اعمال خودکار آن در حقوق همان دوره اضافه شد.
- فیلتر دوره، جستجو و مرتب‌سازی برای مرخصی‌ها، وام‌ها و مساعده‌ها اضافه شد.
- نمایش اطلاعات قبلی پس از همگام‌سازی و خطاهای رابط کاربری اصلاح شدند.

### 🛡️ پایداری و همگام‌سازی

- اعتبارسنجی ورودی‌ها در برنامه و سرور و جلوگیری از عملیات نامعتبر پیش از ارسال تکمیل شد.
- همگام‌سازی مدل‌های جدید حقوق، مرخصی و پیش‌نویس‌ها کامل شد.
- خطاهای Dropdown، UndoHistory، Hero، ListTile و overflow داشبورد رفع شدند.

## 🚀 نسخه 0.5.0 alpha — ۱۴۰۵/۰۴/۰۲ | 2026-06-23

### 📦 عملیات دسته‌ای کارکنان

- نام کارکنان و عنوان ستون‌ها هنگام پیمایش جدول پین شدند و نوار پیمایش افقی همیشه در دسترس قرار گرفت.
- اسکرول عمودی اضافی حذف شد و ورود اعداد در جدول با نمایش فارسی اصلاح شد.
- وابستگی میان تب‌ها کامل شد؛ تغییر مشخصات کارمند مانند تعداد فرزند، مزایای مرتبط را مانند فرم تکی بازمحاسبه می‌کند.
- گزینه نوبت‌کاری به اطلاعات حقوق و مزایای روزانه کارکنان اضافه شد.

### 💳 وام و اقساط

- مبلغ قسط، تعداد اقساط و مانده وام به‌صورت پویا به یکدیگر متصل شدند.
- تعداد اقساط اعشاری و قسط پایانی غیررند در برنامه و سرور پشتیبانی می‌شود.

### 🩺 مرخصی و استعلاجی

- استعلاجی به ثبت فیش تکی، عملیات دسته‌ای، فیش حقوقی، گزارش‌ها و همگام‌سازی سرور اضافه شد.
- اعتبارسنجی روزهای مرخصی و استعلاجی در برنامه و سرور اعمال شد.
- گزینه محاسبه مرخصی و توضیح استعلاجی فقط هنگام وجود مقدار مرتبط نمایش داده می‌شوند.

### 🧮 محاسبه حقوق

- نوبت‌کاری ثبت‌شده در پروفایل کارمند به‌صورت پیش‌فرض در فیش فعال می‌شود و غیرفعال‌کردن آن هشدار دارد.
- امکان تعیین مبنای روزانه دستی برای اضافه‌کاری اضافه شد و انتخاب هر کارمند برای ماه‌های بعد حفظ می‌شود.
- اطلاعات در حال ثبت فیش به‌صورت پیش‌نویس محلی و سروری ذخیره می‌شود و برای دوره‌های بعد از آخرین ماه موجود الگو می‌گیرد.

### 📅 تاریخ و ورود اطلاعات

- سال و ماه پیش‌فرض فرم‌ها و تقویم‌ها بر اساس تاریخ جاری جلالی تنظیم می‌شوند.
- انتخاب ماه و سال با اسکرول موس امکان‌پذیر شد.
- عبارت‌های اضافی «مطابق اکسل» از ساخت فیش حذف شدند.

### 🪟 پایداری ویندوز و همگام‌سازی

- اجرای هم‌زمان بیش از یک نمونه از برنامه در ویندوز مسدود شد.
- نمایش تکراری بخش دسترسی‌پذیری در تنظیمات اصلاح شد.
- همگام‌سازی فیلدهای جدید کارکنان، فیش‌ها و پیش‌نویس‌ها با سرور تکمیل شد.

### 🧪 کیفیت

- تست‌های محاسبه اضافه‌کاری، استعلاجی، اقساط، تاریخ جلالی، عملیات دسته‌ای و اجرای تک‌نمونه‌ای اضافه شدند.
- تحلیل استاتیک و همه تست‌های برنامه با موفقیت اجرا شدند.

## 🚀 نسخه 0.4.0 alpha — ۱۴۰۵/۰۴/۰۲

### 📦 عملیات دسته‌ای

- بخش ورود دسته‌ای کارکنان به دو حالت «نمایش و ویرایش اطلاعات قبلی» و «ثبت اطلاعات جدید» تقسیم شد.
- در حالت نمایش اطلاعات قبلی، امکان ویرایش، حذف، کپی‌کردن و افزودن ردیف‌های جدید کنار داده‌های موجود فراهم شد.
- مقادیر خودکار، وابستگی عددها و تیک‌های قابل روشن/خاموش مانند فرم تک‌کارمند در جدول دسته‌ای اعمال می‌شوند.
- ستون‌های تاریخ با اعداد فارسی نمایش داده می‌شوند.

### 🪟 تجربه ویندوز

- منوی سمت راست ویندوز به‌صورت نرم و انیمیشنی جمع و باز می‌شود.
- حالت جمع‌شده به نوار آیکونی تبدیل می‌شود و وضعیت آن برای اجرای بعدی ذخیره می‌ماند.
- هدر، فوتر، انتخاب تم و Tooltipهای منو برای حالت فشرده بهینه شدند.

### ♿ دسترسی‌پذیری و ظاهر

- گزینه «اندازه رابط برنامه» اضافه شد تا کل UI برنامه کوچک و بزرگ شود.
- تگ‌های «جدید» از بخش‌های تنظیمات حذف شدند.
- ظاهر تمام Sliderهای برنامه به طراحی Material 3 Expressive اندروید 16 تغییر کرد.
- تنظیمات قدیمی دسترسی‌پذیری با مقدار پیش‌فرض امن برای مقیاس رابط سازگار می‌مانند.

### 🧪 کیفیت

- تست‌های جدید برای عملیات دسته‌ای، منوی جمع‌شونده، مقیاس رابط و ظاهر جدید Slider اضافه شد.
- اجرای موفق `flutter analyze`.
- اجرای موفق `flutter test`.

### ⚙️ انتشار

- نسخه برنامه به `0.4.0 alpha` ارتقا یافت.
- انتشار از طریق GitHub Actions و تگ نسخه به‌صورت Pre-release انجام می‌شود.

## 🚀 نسخه 0.3.9 alpha — ۱۴۰۵/۰۴/۰۲

### 📦 عملیات دسته‌ای

- ورود دسته‌ای کارکنان با جدول ستونی داخل برنامه به نسخه ویندوز اضافه شد.
- عملیات دسته‌ای به سه بخش ورود کارکنان، محاسبه دستی حقوق و محاسبه و چاپ دسته‌ای تقسیم شد.
- ثبت گروهی کارکنان به ذخیره محلی تراکنشی و همگام‌سازی یکپارچه با سرور متصل شد.
- اسکرول افقی و عمودی جدول‌های عریض همیشه در دسترس قرار گرفت.

### 🪟 تجربه کاربری

- بخش راهنما و پشتیبانی از برنامه حذف شد.
- برنامه در ویندوز از اولین فریم به‌صورت Maximized اجرا می‌شود.
- پیام‌های خطای ورود به فارسی و متناسب با نوع خطا نمایش داده می‌شوند.

### ⚙️ انتشار

- نسخه برنامه به `0.3.9 alpha` ارتقا یافت.
- GitHub Actions روی شاخه `master` و tagهای نسخه اجرا می‌شود.
- مراحل تکراری Analyze/Test از اکشن حذف و کش Flutter، Pub، Gradle، Android و Windows فعال شد.

## 🚀 نسخه 0.3.5 alpha — ۱۴۰۵/۰۳/۲۵

### 🪟 پنجره ویندوز

- ✨ ظاهر border و titlebar ویندوز بازطراحی شد و به شکل اختصاصی برنامه درآمد.
- 📌 دکمه Pin اضافه شد تا پنجره همیشه روی بقیه پنجره‌ها بماند.
- 🎨 لوگوی برنامه در titlebar قرار گرفت و رنگ آن با تم، حالت روشن/تاریک و رنگ انتخابی کاربر هماهنگ می‌شود.
- 🧭 رنگ border، دکمه‌ها و نوار بالای پنجره با تم برنامه sync شد.

### 🚪 بستن برنامه و Tray

- 🛠 دیالوگ بستن برنامه دوباره فعال شد و هنگام کلیک روی دکمه X از کاربر می‌پرسد برنامه به tray برود یا کامل بسته شود.
- ✅ گزینه «همیشه همین کار را انجام بده» اصلاح شد و انتخاب کاربر در تنظیمات ذخیره می‌شود.
- 🧯 گزینه Close در منوی tray امن شد و دیگر مسیر خروج باعث کرش برنامه نمی‌شود.
- 🧩 مشکل `No Overlay widget found` در titlebar ویندوز رفع شد.

### 💸 مساعده کارکنان

- 🧹 نمایش کد پرسنلی داخل پرانتز از ستون «کارمند» در لیست مساعده حذف شد تا نام کارمند تمیزتر دیده شود.

### 🧪 کیفیت و پایداری

- 🧪 تست‌های مربوط به ذخیره رفتار بستن پنجره و وضعیت Pin اضافه شد.
- ✅ اجرای موفق `flutter analyze`.
- ✅ اجرای موفق `flutter test`.

### 🏗 انتشار

- 🏷 نسخه برنامه به `0.3.5 alpha` ارتقا یافت.
- ⚙️ GitHub Actions فقط روی tagهای نسخه اجرا می‌شود و خروجی‌ها را به‌صورت Pre-release می‌سازد.
