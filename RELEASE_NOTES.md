**۱۴۰۵/۰۴/۰۷ | 2026-06-28**

## 🪟 آپدیت ویندوز
- مکانیزم آپدیت خودکار ویندوز به Velopack منتقل شد تا نصب بعد از بسته‌شدن برنامه توسط updater مستقل ادامه پیدا کند.
- بعد از تایید نصب، برنامه بسته می‌شود، آپدیت نصب می‌شود و نسخه جدید به‌صورت خودکار دوباره اجرا می‌شود.
- مسیر zip قدیمی برای کاربران نسخه‌های قبلی به‌عنوان fallback باقی ماند تا مهاجرت به نسخه جدید قطع نشود.

## 🚀 انتشار ویندوز
- خروجی‌های Velopack شامل پکیج نصب و فایل‌های feed کنار خروجی portable ویندوز در pre-release منتشر می‌شوند.
- اکشن انتشار برای ساخت helper مستقل ویندوز، بسته‌بندی Velopack و انتشار فایل‌های لازم آماده شد.
- کانال آپدیت ویندوز روی `win` تنظیم شد.

## 🧪 کیفیت و انتشار
- نسخه برنامه به `0.9.10 alpha` ارتقا یافت.
- اکشن انتشار روی شاخه `master` اجرا می‌شود و خروجی به‌صورت pre-release منتشر می‌شود.

**۱۴۰۵/۰۴/۰۷ | 2026-06-28**

## 🧾 فیش‌های حقوق
- فیش‌های ذخیره‌شده با تغییر مرخصی، وام یا مساعده به‌صورت خودکار تغییر نمی‌کنند.
- در لیست فیش‌های حقوقی، در صورت تغییر وضعیت مرخصی، وام یا مساعده، دکمه «آپدیت فیش» نمایش داده می‌شود تا کاربر با تایید خودش فیش را به‌روزرسانی کند.
- گزینه «ویرایش فیش» در ستون عملیات فیش‌ها دوباره قابل مشاهده شد و اکشن‌های موبایل بدون بیرون‌زدگی نمایش داده می‌شوند.

## 🖨️ چاپ و خروجی فیش
- خروجی PDF فیش با حاشیه امن بیشتر ساخته می‌شود تا گوشه‌های فیش در پرینترها حذف نشود.
- ردیف «رند حقوق» در پیش‌نمایش و PDF چاپی واضح‌تر شد.
- بخش امضای کارمند، حسابدار و مدیریت به PDF چاپی اضافه شد.

## 🧪 کیفیت و انتشار
- نسخه برنامه به `0.9.9 alpha` ارتقا یافت.
- مراحل جداگانه `flutter pub get` از اکشن انتشار حذف شد و بیلدها مستقیم اجرا می‌شوند.
- اکشن انتشار روی شاخه `master` اجرا می‌شود و خروجی به‌صورت pre-release منتشر می‌شود.

**۱۴۰۵/۰۴/۰۶ | 2026-06-27**

## 🧾 چاپ و خروجی فیش
- خروجی PDF فیش‌ها برای A4، A5 و B5 به حالت عمودی و اندازه واقعی همان کاغذ اصلاح شد.
- اعداد داخل فیش چاپی و خروجی تصویری فارسی شدند.
- خروجی عکس فیش از PNG به JPG با پس‌زمینه سفید تغییر کرد تا در نمایشگرها و پرینت، پس‌زمینه شفاف نداشته باشد.
- کیفیت خروجی عکس با رندر DPI بالاتر و ذخیره JPG با کیفیت بیشتر ارتقا پیدا کرد.

## 🪟 آپدیت ویندوز
- نصب‌کننده آپدیت ویندوز بعد از بسته شدن برنامه تا آزاد شدن فایل اجرایی صبر می‌کند و کپی فایل‌ها را با retry انجام می‌دهد.
- پیام «آپدیت شد» فقط وقتی نمایش داده می‌شود که نصب‌کننده ویندوز marker موفقیت را بعد از جایگزینی فایل‌ها ثبت کرده باشد.
- پنجره‌های آپدیت با کلیک بیرون یا دکمه برگشت بسته نمی‌شوند و فقط گزینه «بعدا» آن‌ها را می‌بندد.

## 🧾 جدول‌های ویندوز
- عرض پیش‌فرض ستون‌های جدول‌ها متعادل‌تر شد تا ستون‌های عددی و متنی بی‌دلیل بزرگ یا کوچک نباشند.
- امکان تغییر اندازه ستون‌ها با موس از روی هدر جدول اضافه شد.

## 🧪 کیفیت و انتشار
- نسخه برنامه به `0.9.6` ارتقا یافت.
- اکشن انتشار روی شاخه `master` اجرا می‌شود و خروجی به‌صورت pre-release منتشر می‌شود.

**۱۴۰۵/۰۴/۰۶ | 2026-06-27**

## 🔐 ورود و ثبت نام
- امکان نمایش و مخفی کردن رمز عبور در فرم ورود اپلیکیشن و پنل مدیریت اضافه شد.
- صفحه شروع اپلیکیشن به سه گزینه «ثبت نام»، «ورود» و «پشتیبانی» تغییر کرد.
- درخواست ثبت‌نام از اپلیکیشن به سرور ارسال می‌شود و ادمین می‌تواند آن را به شرکت و سطح دسترسی مناسب وصل کند.
- بعد از تایید ادمین، اپلیکیشن با باز شدن دوباره تلاش می‌کند کاربر را خودکار وارد برنامه کند.

## 🖨️ چاپ و خروجی فیش
- هنگام چاپ فیش تکی و دسته‌ای، انتخاب اندازه A4، A5 یا B5 اضافه شد.
- خروجی عکس فیش‌ها هم با انتخاب اندازه A4، A5 یا B5 ساخته می‌شود.
- خروجی عکس از رندر PDF ساخته می‌شود تا تمام بخش‌های فیش کامل و هم‌اندازه خروجی چاپ باشد.

## 🔄 نشست و همگام‌سازی
- refresh توکن در اپلیکیشن و پنل مدیریت در برابر درخواست‌های هم‌زمان مقاوم شد تا خطای unauthorized ناگهانی رخ ندهد.
- نمایش درخواست انتقال دیتای لوکال در اولین ورود حذف شد و داده‌ها همیشه ابتدا از سرور دریافت و ذخیره می‌شوند.

## 🧪 کیفیت و انتشار
- نسخه برنامه به `0.9.5` ارتقا یافت.
- تحلیل کد و تست‌های اپلیکیشن، سرور و پنل مدیریت با موفقیت اجرا شدند.
- انتشار روی شاخه `master` اجرا می‌شود و خروجی به‌صورت pre-release منتشر می‌شود.
